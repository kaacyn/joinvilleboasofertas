# JBO PWA instalável — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o JBO instalável na tela inicial (manifesto, ícones, service worker, prompt iOS/Android), no mesmo espírito do Snap.

**Architecture:** `@vite-pwa/nuxt` em `injectManifest` com SW próprio em `public/sw.js` (Workbox + denylist `/api/`). Ícones gerados do logo atual. Prompt de instalação no header, sem BottomNav. Push handlers entram no plano do sino, não nesta fatia.

**Tech Stack:** Nuxt 4, `@vite-pwa/nuxt`, Workbox, Vitest, sharp/script de ícones.

## Global Constraints

- Sem login, sem BottomNav, sem FAB, sem cache de `/api/` nem `/api/img/`.
- `theme_color` e `background_color`: `#0D131D`.
- `name`: Joinville Boas Ofertas; `short_name`: JBO; `display: standalone`; `lang: pt-BR`; `start_url: /`; `scope: /`.
- Referência de install: `/root/Docker/projetos/snap/src/composables/usePwaInstall.js` e modais iOS/Android.
- Referência de SW: `/root/Docker/projetos/snap/src/sw/sw.js` (sem badge/inbox nesta fatia).
- Preservar WIP alheio; commitar só arquivos desta fatia.
- Spec: `docs/superpowers/specs/2026-08-17-jbo-pwa-push-share-design.md`.

---

### Task 1: Módulo PWA e contrato no nuxt.config

**Files:**
- Modify: `/root/Docker/projetos/dev-joinvilleboasofertas/package.json`
- Modify: `/root/Docker/projetos/dev-joinvilleboasofertas/nuxt.config.ts`
- Test: `/root/Docker/projetos/dev-joinvilleboasofertas/tests/pwaConfig.spec.ts`

**Interfaces:**
- Produces: módulo `@vite-pwa/nuxt` com `injectManifest`, `registerType: 'autoUpdate'`, `filename: 'sw.js'`
- Produces: `manifest.name === 'Joinville Boas Ofertas'`

- [ ] **Step 1: Escrever o teste que lê o nuxt.config**

```ts
import { existsSync, readFileSync } from 'node:fs'
import { resolve } from 'node:path'
import { describe, expect, it } from 'vitest'

const root = resolve(import.meta.dirname, '..')
const source = (path: string) => readFileSync(resolve(root, path), 'utf8')

describe('PWA JBO', () => {
  it('registra @vite-pwa/nuxt em injectManifest com identidade JBO', () => {
    const pkg = source('package.json')
    expect(pkg).toContain('@vite-pwa/nuxt')

    const config = source('nuxt.config.ts')
    expect(config).toContain('@vite-pwa/nuxt')
    expect(config).toContain("injectManifest")
    expect(config).toContain("short_name: 'JBO'")
    expect(config).toContain("theme_color: '#0D131D'")
    expect(config).toContain("display: 'standalone'")
    expect(config).toMatch(/denylist:[\s\S]*\/api\//)
  })

  it('tem service worker fonte para injectManifest', () => {
    expect(existsSync(resolve(root, 'app/sw.ts')) || existsSync(resolve(root, 'public/sw.js'))).toBe(true)
  })
})
```

- [ ] **Step 2: Rodar e confirmar RED**

Run: `npm test -- tests/pwaConfig.spec.ts`  
Expected: FAIL — `@vite-pwa/nuxt` ausente.

- [ ] **Step 3: Instalar o módulo e configurar**

```bash
npm install -D @vite-pwa/nuxt
```

Em `nuxt.config.ts` adicionar o módulo e o bloco `pwa` (injectManifest, manifesto, `devOptions.enabled` só se o host loc exigir, igual ao Snap). Fonte do SW: `app/sw.ts` se o módulo Nuxt exigir; senão `public/sw.js` — um arquivo só.

- [ ] **Step 4: Confirmar GREEN e commit**

```bash
npm test
git add package.json package-lock.json nuxt.config.ts app/sw.ts tests/pwaConfig.spec.ts
git commit -m "feat: registra PWA instalável no JBO"
```

---

### Task 2: Ícones e metas Apple

**Files:**
- Create: `/root/Docker/projetos/dev-joinvilleboasofertas/scripts/generate-pwa-icons.mjs` (adaptar `snap/scripts/generate-pwa-icons.mjs`)
- Create: `public/pwa-192x192.png`, `public/pwa-512x512.png`, `public/pwa-maskable-512x512.png`, `public/apple-touch-icon.png`
- Modify: `nuxt.config.ts` (`apple-touch-icon` no `app.head.link`)
- Modify: `package.json` script `"icons": "node scripts/generate-pwa-icons.mjs"`

- [ ] **Step 1: Falhar o teste se os PNGs não existem**

No mesmo `pwaConfig.spec.ts`:

```ts
it('expõe ícones PWA 192/512/maskable e apple-touch-icon', () => {
  for (const file of [
    'public/pwa-192x192.png',
    'public/pwa-512x512.png',
    'public/pwa-maskable-512x512.png',
    'public/apple-touch-icon.png',
  ]) {
    expect(existsSync(resolve(root, file))).toBe(true)
  }
  expect(source('nuxt.config.ts')).toContain('apple-touch-icon')
})
```

- [ ] **Step 2: RED → gerar ícones a partir de `public/assets/logo.png` → GREEN → commit**

```bash
git commit -m "feat: gera ícones PWA do logo JBO"
```

---

### Task 3: Prompt de instalação

**Files:**
- Create: `app/composables/usePwaInstall.ts` (portar detecção iOS/Android/standalone/`beforeinstallprompt` de `snap/src/composables/usePwaInstall.js`, sem auth)
- Create: `app/components/pwa/IosInstallModal.vue`
- Create: `app/components/pwa/AndroidInstallModal.vue`
- Create: `app/components/pwa/InstallAppButton.vue` (botão no header, oculto se `standalone`)
- Modify: `app/components/AppHeader.vue` — botão à esquerda do `HeaderMenu`
- Test: `tests/pwaInstall.spec.ts`

**Interfaces:**
- Produces: `usePwaInstall()` → `{ canInstall, isStandalone, isIos, promptInstall }`
- `promptInstall()` chama `deferredPrompt.prompt()` no Android; abre modal iOS senão

- [ ] **Step 1: Teste de source**

```ts
it('expõe o CTA de instalar no header e os modais iOS/Android', () => {
  const header = source('app/components/AppHeader.vue')
  expect(header).toContain('InstallAppButton')
  expect(existsSync(resolve(root, 'app/composables/usePwaInstall.ts'))).toBe(true)
  expect(source('app/composables/usePwaInstall.ts')).toContain('beforeinstallprompt')
  expect(source('app/composables/usePwaInstall.ts')).toContain('display-mode: standalone')
})
```

- [ ] **Step 2: RED → implementar composable + botão 44px + modais (copy em pt-BR: “Adicionar à Tela de Início”) → GREEN → commit**

```bash
git commit -m "feat: prompt de instalação PWA no header do JBO"
```

Não copiar BottomNav, FAB, login nem toast de push.

---

## Verificação da fatia

Rebuild `jbo-dev-nuxt` e abrir `/`: manifesto em `/manifest.webmanifest`, SW registra, header mostra CTA fora de standalone. Chrome Android: “Adicionar à tela inicial”. iOS Safari: modal com os passos.
