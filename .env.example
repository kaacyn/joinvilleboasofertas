# Porta no host para acesso local (padrão: 8090)
WEB_PORT=8090

# Token do Cloudflare Tunnel — necessário para expor o domínio publicamente.
# Crie o tunnel em: Cloudflare Zero Trust → Networks → Tunnels
# Rota pública: joinvilleboasofertas.com → http://web:80
CF_TUNNEL_TOKEN=
